import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        padding: 30
    },
    textInput:{
        borderWidth: 1,
        alignContent: 'center',
    },
});